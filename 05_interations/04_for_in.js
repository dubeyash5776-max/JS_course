const myObject = {
    js: 'javascript',
    cpp: 'c++',
    py: 'python'
}

for (const key in myObject) {
    // console.log(`${key} is shortcut is for ${myObject[key]}`);
    
}

const programming = ["js", "cpp", "ruby", "python"]

for (const key in programming){
    console.log(programming[key]);
    
}